/**
 * Test case for fileAccess.
 * Runs with mocha.
 */
'use strict'

const fileAccess = require('../lib/helpers/file_access.js')
const assert = require('assert')

describe('file-access', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('File access', async () => {

  })
})

/* global describe, before, after, it */
