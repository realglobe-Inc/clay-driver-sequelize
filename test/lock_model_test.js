/**
 * Test case for lockModel.
 * Runs with mocha.
 */
'use strict'

const lockModel = require('../lib/modeling/lock_model.js')
const assert = require('assert')

describe('lock-model', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Lock model', async () => {

  })
})

/* global describe, before, after, it */
