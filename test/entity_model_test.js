/**
 * Test case for entityModel.
 * Runs with mocha.
 */
'use strict'

const entityModel = require('../lib/modeling/entity_model.js')
const assert = require('assert')
const co = require('co')

describe('entity-model', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Entity model', () => co(function * () {

  }))
})

/* global describe, before, after, it */
