/**
 * Test case for normalizer.
 * Runs with mocha.
 */
'use strict'

const normalizer = require('../lib/helpers/normalizer.js')
const assert = require('assert')


describe('normalizer', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Normalizer', async () => {

  })
})

/* global describe, before, after, it */
