/**
 * Test case for queuePool.
 * Runs with mocha.
 */
'use strict'

const queuePool = require('../lib/helpers/queue_pool.js')
const assert = require('assert')
const co = require('co')

describe('queue-pool', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Queue pool', () => co(function * () {

  }))
})

/* global describe, before, after, it */
