/**
 * Test case for attributeModel.
 * Runs with mocha.
 */
'use strict'

const attributeModel = require('../lib/modeling/attribute_model.js')
const assert = require('assert')
const co = require('co')

describe('attribute-model', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Attribute model', () => co(function * () {

  }))
})

/* global describe, before, after, it */
