/**
 * Test case for usageModel.
 * Runs with mocha.
 */
'use strict'

const usageModel = require('../lib/modeling/usage_model.js')
const assert = require('assert')

describe('usage-model', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Usage model', async () => {

  })
})

/* global describe, before, after, it */
